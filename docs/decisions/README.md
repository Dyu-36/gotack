# Quyết định kiến trúc (ADR)

Mỗi tệp ghi một quyết định: bối cảnh, lựa chọn, hệ quả. Không sửa quyết định cũ,
thêm quyết định mới thay thế nó.

| Số | Quyết định |
| --- | --- |
| 0001 | Lớp desktop mỏng, engine giữ toàn bộ logic |
| 0002 | Giữ nguyên tên thư mục `frontend/` |
| 0003 | Vendored Crush ở `third_party/` và tự viết client REST + SSE |

Đặt tên tệp: `NNNN-tieu-de-ngan.md`, số tăng dần, không dùng lại số cũ.
