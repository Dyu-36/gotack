// Package session drives conversation flow for the UI.
//
// The engine owns history and state; this package orchestrates calls and keeps
// only the small render cache the UI needs.
package session
