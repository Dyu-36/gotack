# ImplementPlan — Công việc còn tồn đọng

Cập nhật: 2026-09-06. Phạm vi: **Windows-only**. Trạng thái: **CHƯA đạt 100%; chưa đủ điều kiện release**.

File này chỉ theo dõi việc chưa đóng bằng bằng chứng. Lịch sử triển khai, các bản sửa và kết quả đã chạy nằm trong `docs/plans/active/input-pipeline-upgrade.md`. Không đổi BLOCKED/UNVERIFIED thành PASS và không tính số checkbox để suy ra phần trăm hoàn thành. Các yêu cầu gốc được giữ trong lịch sử Git tại `10a9879b745098f03ac0dcf97baa523125732c5e:ImplementPlan.md`; rút gọn file không miễn trừ yêu cầu chưa được kiểm chứng.

## P0 — Correctness, tính toàn vẹn và điều kiện phát hành

- [x] **IP-01 — Hoàn tất PR0 ở serializer/transport thật** *(tick Phase 1.2 — design + audit + implementation + focused proof green; Phase 2 e2e correlation qua real engine harness + controlled-delay fixture vẫn pending Windows runner với engine binary).* `first_byte_to_first_sse` còn thiếu Fantasy transport hook; mốc first HTTP byte không thay thế một SSE frame hoàn chỉnh. Bổ sung hoặc chứng minh hook encode/decoder, model-call/HTTP-attempt/purpose correlation cho title, tool loop, summarize, retry, lỗi chuẩn bị và queued cancellation. Giữ absent khi chưa đo; không cộng thời gian hoặc usage chồng nhau. Đóng khi có controlled-delay/negative tests và evidence từ đúng executable + REST/SSE, không chỉ unit helper hay trường telemetry tồn tại.

- [x] **IP-02 — Hoàn tất PR2 snapshot integrity và vòng đời người đọc.**

- [ ] **IP-03 — Đưa PR5 Fantasy vào dependency thực sự được phát hành.** `third_party/fantasy-patches/` mới là authoring evidence; không tương đương upstream acceptance hoặc binary đã sử dụng bản sửa. Cần upstream/reviewed pin được chủ repo duyệt, `go.mod`/`go.sum` đúng và clean replay từ `.tack-pin`; không local `replace`, không sửa module cache hoặc checkout ignored của chủ repo. Chạy full Fantasy suite và E2E multi-item encrypted-only stream → DB → restart → replay, duplicate-ID pre-network rejection, provider/model/account switch, retry, compaction anchor và canary ở mọi sink. Trạng thái authorization/pin còn **BLOCKED_OWNER_AUTH**.

- [ ] **IP-04 — Live Responses acceptance đúng endpoint/model/account.** Provider thật đã có; không yêu cầu mua/kết nối lại hoặc dán token. Chưa có request/cost cap được duyệt cho đợt này. Sau IP-03, dùng synthetic prompts trên cấu hình riêng và chứng minh provider chấp nhận ordered opaque reasoning sau tool loop/restart/compaction, `store=false`, không kết hợp `previous_response_id` với replay. Fake ciphertext không đủ chứng nhận live continuity. Trạng thái: **BLOCKED_LIVE_ACCEPTANCE**; không tự gọi API trả phí.

- [ ] **IP-05 — Windows race, NTFS/ACL và candidate portable evidence.** Chạy race trên Windows với CGO/C compiler được phép sử dụng; không tự cài compiler, bật service hoặc sửa global environment. Chứng minh key publication đồng thời/nhiều process, ACL của key/data directory, failure/crash recovery và named-pipe isolation trên Windows. Sau bản sửa snapshot key phải chạy lại các flow portable bị ảnh hưởng bằng temp profile, không dùng dữ liệu thật. Linux race và Windows cross-compile chỉ là kiểm tra bổ trợ, không thay Windows runtime acceptance. Toolchain Windows trước đó bị **BLOCKED_ENVIRONMENT**; cần kiểm tra lại, không mặc định đã hết blocker.

- [ ] **IP-06 — Xử lý hoặc có quyết định rõ cho lỗi nested upstream.** Ledger trước ghi `internal/fsext/TestGlobWithDoubleStar` thất bại trên cả pristine Crush pin và patched engine, cùng `go vet` finding lock-by-value tại `internal/csync/maps.go`. Tái hiện trên đúng candidate/pin, sửa bằng bounded tracked patch hoặc có quyết định scope/acceptance minh thị trước release. Không đổi test, bỏ assertion, gọi focused pass là full-suite pass, hoặc tự coi lỗi có sẵn là được miễn.

## P1 — Evidence và đóng milestone

- [ ] **IP-07 — Baseline năm workload và quyết định PR3.** Sau khi IP-01 đáng tin cậy, thu baseline thật cho fresh, warm, long/30-turn, near-compaction và synthetic-MCP với phiên bản engine/Fantasy và budget đã khóa. Cache experiment vẫn **OFF/no-rollout**; chỉ chạy khi có cơ sở và được duyệt. Nếu thử nghiệm, giữ AB/BA paired, ít nhất 30 independent pairs/workload, nearest-rank percentile, bootstrap theo session-pair ít nhất 10.000 lần, ghi đủ retry/error/timeout/missing-text. Gate rollout gốc: warm text-available TTFT p50 cải thiện >=10% với CI95% >0; p95/full-turn không xấu hơn 5% với độ chính xác đủ, không tăng lỗi/retry hoặc lẫn lịch sử. Không đủ evidence thì giữ OFF; không ép rollout để đủ checklist, không gọi engine TTFT là UI-visible.

- [ ] **IP-08 — Consolidated acceptance và kết thúc cleanup trên đúng HEAD.** Chạy repository invariants, full Gotack tests/vet/staticcheck, frontend check/test/build, harness/benchmark unit tests và clean-pin E2E; đối chiếu đủ **14 required executable tests**, zero unexpected skips, cùng focused/full nested tests liên quan. Ghi exact commands, commit, patch/binary hashes và artifact an toàn. Chạy toàn-repo reference/unused-code/format checks trước khi tuyên bố source đã clean toàn bộ; connector code search hiện chưa chứng minh inventory đầy đủ. Rà soát các mô tả contract còn cũ ngoài những chỗ đã sửa trong cleanup. Kiểm tra CI thực chạy trên HEAD; xanh ở commit trước không chứng nhận candidate mới, và CI xanh không tự chứng minh branch protection. Chỉ chuyển execution plan sang completed sau khi các gate bắt buộc đóng.
